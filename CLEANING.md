# 项目清理指南

## 清理命令

### 完整清理 (推荐)
```bash
npm run clean
```

这个命令会：
- 删除 `dist/` 目录 (webpack 构建输出)
- 删除 `out/` 目录 (TypeScript 编译输出) 
- 删除 `.vscode-test/` 目录 (VS Code 测试缓存，通常很大)
- 删除 `*.vsix` 文件 (VS Code 扩展包)
- 显示每个被删除目录/文件的大小
- 显示总共释放的磁盘空间

### 快速清理
```bash
npm run clean:fast
```

使用 rimraf 直接删除，不显示详细信息，速度更快。

## 为什么需要清理？

1. **`.vscode-test/`** - 包含多个 VS Code 版本的完整安装包，通常占用 1-2GB 空间
2. **`dist/` 和 `out/`** - 构建和编译产物，可以重新生成
3. **`*.vsix`** - 扩展打包文件，如果不需要分发可以删除

## 清理后的影响

- ✅ 节省磁盘空间 (通常 1-2GB)
- ✅ 不影响源代码
- ✅ 下次运行测试时会重新下载必要的 VS Code 版本
- ✅ 构建和编译可以随时重新执行

## 何时使用清理命令？

- 开发完成后准备提交代码
- 磁盘空间不足
- 更换开发环境
- 排除缓存相关的问题

## 注意事项

- 清理后第一次运行测试可能需要较长时间（重新下载 VS Code）
- 如果有自定义的测试配置，请确保备份重要配置